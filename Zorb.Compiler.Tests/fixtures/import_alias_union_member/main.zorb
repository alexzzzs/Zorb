import "lib.zorb" as util

fn main() -> i64 {
    value: util.Value = util.Value{ Number: 7 }

    switch value.tag {
        case util.Value.Tag.Number {
            return value.Number
        }
        case util.Value.Tag.Flag {
            return 0
        }
    }
}

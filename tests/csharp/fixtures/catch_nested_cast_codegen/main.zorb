error Fail = 100

fn maybe_ptr(flag: i64) !*u8 {
    if flag != 0 {
        return error.Fail
    }

    return cast(*u8, 0)
}

fn main() -> i64 {
    ptr: *i8 = cast(*i8, maybe_ptr(0) catch |err| {
        return err
    })
    return cast(i64, ptr)
}

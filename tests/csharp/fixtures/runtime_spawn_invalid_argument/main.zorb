import "../../../../runtime/std/io.zorb"
import "../../../../runtime/std/os.zorb"
import "../../../../runtime/std/task.zorb"
import "../../../../runtime/std/mem.zorb"
import "../../../../runtime/std/errors.zorb"

fn job(arg: *void) {}

fn _start() {
    result: i32 = std.task.spawn(cast(*std.mem.HeapAllocator, 0), job, cast(*void, 0)) catch |err| {
        if err == error.InvalidArgument {
            std.io.print("invalid arg\n")
            std.os.exit(0)
            return
        }

        std.os.exit(err)
        return
    }

    std.io.print("unexpected\n")
    std.os.exit(1)
}

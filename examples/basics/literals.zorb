struct Pair {
    left: i32,
    right: i32
}

fn main() {
    pair: Pair = Pair{ left: 1, right: 2 }
    mask: [4]u8 = [4]u8{ 1, 1, 0, 0 }
    copy: [4]u8 = mask
    ready: bool = (pair.left == 1 && copy[0] == 1) || false
}
